# ==========================================================================
# SALES DATA GENERATION MODELS
# ==========================================================================
#
# This file controls *how* the synthetic sales rows behave over time:
#   - How many rows land in each month (macro demand / growth)
#   - How many items per order (quantity / basket size)
#   - How prices drift with inflation and get snapped to realistic values
#   - Customer mix: new vs returning buyers, and when they appear
#   - Returns: rate, reasons, lag distribution
# ==========================================================================

models:

  # ================================================================
  # QUANTITY — items per order (basket size)
  # ================================================================
  quantity:
    base_poisson_lambda: 1.7          # average basket before monthly factors
    min_qty: 1
    max_qty: 8

    # Monthly seasonal multipliers (12 values, Jan–Dec).
    monthly_factors:
      - 0.99
      - 0.98
      - 1.00
      - 1.00
      - 1.01
      - 1.02
      - 1.02
      - 1.01
      - 1.00
      - 1.03
      - 1.06
      - 1.05

    noise_sigma: 0.12                 # per-row gaussian jitter


  # ================================================================
  # PRICING — inflation drift + realistic price snapping
  # ================================================================
  # Pipeline: base price → inflation drift → snap to bands/endings
  #           → apply markdown ladder → enforce caps → round to cents
  # ================================================================
  pricing:
    # --- Inflation ---
    inflation:
      annual_rate: 0.03
      month_volatility_sigma: 0.0     # 0 = deterministic drift
      factor_clip: [1.00, 1.25]       # hard bounds on cumulative factor
      volatility_seed: 123

    # --- Markdown / DiscountAmount ---
    # Independent from Promotions (analysis-time only via PromotionKey).
    markdown:
      enabled: true
      max_pct_of_price: 0.50
      min_net_price: 0.01
      allow_negative_margin: false

      # Discrete discount amounts with weighted random selection.
      ladder:
        - { kind: none, value: 0.0,   weight: 0.35 }
        - { kind: amt,  value: 50.0,  weight: 0.22 }
        - { kind: amt,  value: 100.0, weight: 0.16 }
        - { kind: amt,  value: 200.0, weight: 0.12 }
        - { kind: amt,  value: 300.0, weight: 0.08 }
        - { kind: amt,  value: 400.0, weight: 0.04 }
        - { kind: amt,  value: 500.0, weight: 0.03 }

    # --- Appearance snapping ---
    # Single source of truth for price/cost quantization.
    # Used by BOTH product dimension generation and sales-time inflation.
    # Finer bands → gradual price growth visible in YoY analysis.
    appearance:
      enabled: true

      unit_price:
        rounding: floor
        endings:
          - { value: 0.99, weight: 1.0 }
        bands:
          - { max: 100,  step: 10 }
          - { max: 500,  step: 25 }
          - { max: 2000, step: 50 }
          - { max: 5000, step: 100 }
          - { max: 1e18, step: 250 }

      unit_cost:
        rounding: nearest
        endings:
          - { value: 0.00, weight: 1.0 }
        bands:
          - { max: 100,   step: 5 }
          - { max: 500,   step: 10 }
          - { max: 2000,  step: 25 }
          - { max: 10000, step: 50 }
          - { max: 1e18,  step: 100 }

      discount:
        rounding: floor
        bands:
          - { max: 100,  step: 50 }
          - { max: 1e18, step: 100 }


  # ================================================================
  # MACRO DEMAND — overall row allocation shape per month
  # ================================================================
  # (Consumed by chunk_builder.py, not sales_models.)
  # ================================================================
  macro_demand:
    base_level: 1.0
    yearly_growth: 0.08
    seasonality_amplitude: 0.20
    seasonality_phase: 0.0
    noise_std: 0.04
    eligible_blend: 0.0

    yoy_growth_schedule:
      mode: "repeat"
      values: [0.09, 0.05, 0.4, 0.2, 0.06]
    row_share_of_growth: 0.5

    shock_probability: 0.08
    shock_impact: [-0.15, 0.20]

    early_month_cap:
      enabled: true
      max_rows_per_customer: 25
      redistribute_excess: true


  # ================================================================
  # BRAND POPULARITY — rotating "winner" brand each year
  # ================================================================
  # (Consumed by sales_worker/init.py, not sales_models.)
  # ================================================================
  brand_popularity:
    enabled: true
    seed: 123
    winner_boost: 3.0
    noise_sd: 0.25
    min_share: 0.02
    year_len_months: 12


  # ================================================================
  # CUSTOMERS — new vs returning buyer mix
  # ================================================================
  customers:
    # Fraction of eligible customers that buy each month.
    # Higher = more repeat buyers per month, lower = sparser.
    distinct_ratio: 0.55

    # Approximate share of each month's orders from first-time buyers.
    # 0.05 = 5% new customers, 0.20 = aggressive acquisition.
    new_customer_share: 0.08
    max_new_fraction_per_month: 0.05
    # Multi-year oscillation in the distinct buyer count.
    # 0.0 = flat, 0.35 = ±35% swings over a 24-month cycle.
    cycle_amplitude: 0.40

    # When new customers appear relative to the dataset timeline.
    # -1.0 = heavily front-loaded (most acquisition early)
    #  0.0 = evenly spread
    #  1.0 = heavily back-loaded (accelerating acquisition)
    discovery_shape: 0.0
    
    # Month-to-month random noise on participation count.
    # 0.0 = smooth, 0.12 = visible jitter (~±12%), 0.25 = choppy.
    participation_noise: 0.25

    # Seasonal spikes: specific calendar months get a customer count boost.
    # Mimics real retail: holiday shopping, back-to-school, summer sales.
    seasonal_spikes:
      - { month: 3,  boost: 0.30 }    # spring sale
      - { month: 7,  boost: 0.25 }    # summer clearance
      - { month: 9,  boost: 0.20 }    # back-to-school
      - { month: 11, boost: 0.65 }    # Black Friday / pre-holiday
      - { month: 12, boost: 0.45 }    # holiday shopping

  # ================================================================
  # RETURNS
  # ================================================================
  returns:
    enabled: true

    reasons:
      - { key: 1, label: "Damaged / Defective",       weight: 0.28 }
      - { key: 2, label: "Wrong Item",                weight: 0.12 }
      - { key: 3, label: "Not as Described",          weight: 0.14 }
      - { key: 4, label: "Arrived Late",              weight: 0.08 }
      - { key: 5, label: "Better Price Elsewhere",    weight: 0.06 }
      - { key: 6, label: "No Longer Needed",          weight: 0.20 }
      - { key: 7, label: "Size / Fit",                weight: 0.07 }
      - { key: 8, label: "Other",                     weight: 0.05 }

    lag_days:
      distribution: triangular
      mode: 7

    quantity:
      full_line_probability: 0.85
